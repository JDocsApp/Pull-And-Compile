<?xml version="1.0"?>
<CompileConfig>
    <CommandList>
        <Command></Command>
        <Command></Command>
        <Command></Command>
    </CommandList>

    <Binary>
        <!-- Where the binary will end up within the git project after compilation -->
        <CompiledPath></CompiledPath>

        <!-- Where to move the binary to after compilation is complete -->
        <SaveLocation></SaveLocation>
    </Binary>

</CompileConfig>